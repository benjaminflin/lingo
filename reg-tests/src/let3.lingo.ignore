print_int : Int -> Int;

data Maybe a #p where
  Just    : a -p> Maybe a #p; 
  Nothing : Maybe a #p;

main : Int =
    let j : Maybe @Int #Unr = Just 10 #Unr in
    let cs x : Maybe Int #Unr -> Int
        = case x of
            Just i -> print_int i;
            _ -> 0;
    in
    cs j;